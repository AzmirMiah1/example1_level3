package week10_arrays;
public class Lecture10_2dArrays 
{
    public static void multiplicationTable()
    {
        int colum;
        int row;
        int Xsize=10;
        int Ysize=10;
        int [][]table=new int[Xsize][Ysize];
        table[0][0]=1;
        //Create table entries
        
        for(row=0;row<Xsize;row++)
        
            for(colum=0;colum<Ysize;colum++)
            {
                table[row][colum]=row*column;
            }
         for(row=0;row<Xsize;row++)
        
            for(colum=0;colum<Ysize;colum++)
            {
                table[row][colum]=row*column;
            }
         table[row][colum]=row*column;
    }
}
        //PRINT
        for[row][column]=row*column;
        {
        
        
        
        
    }
    //print
    
    
public static void main(String[] args)
{
    multiplicationTable();
 }
}
